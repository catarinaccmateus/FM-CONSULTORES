const Handlebars = require('handlebars');

/*
Handlebars.registerHelper('mobileWindow', function(windowWidth, options) {
console.log('heeere', windowWidth);
console.log('thiiis', this);
if (windowWidth < 768) {
  return options.fn(this);
}
return options.inverse(this);
});

//Handlebars.registerHelper('windowWidth', window.screen.width);
*/